# Data (user data — portable)
data/
dist/
build/
# Build output for GitHub Release assets (patch/full zips + version.json) —
# uploaded directly to Releases, never committed to the source repo (large binaries)
release/
__pycache__/
*.pyc
*.egg-info/
rvc_models/
*.pth
*.index

# OS
Thumbs.db
.DS_Store

# IDE
.vscode/
.idea/
*.swp

# Large files
*.whl
site-packages/
ffmpeg.exe
test_*.wav

# Secrets — never commit (real OAuth client secrets embedded here for the shipped app)
twitch_oauth.py
kick_oauth.py
youtube_oauth.py

# Supporters backend (PHP, deployed separately to men9ch.com) — unrelated to
# app version releases on GitHub, keep entirely out of this repo
server/

# Contains a live client_secret — delete after deploying, never commit
YOUTUBE_TOKEN_DEPLOY.md
